on:
  workflow_dispatch:
    inputs:
      sleep:
        default: '0'
        type: number
        description: 'Time to pause'
        
jobs:
  nothing:
    runs-on: self-hosted
    steps:
      - run: |
          pwd
          whoami
          ls -l
          sleep ${{ inputs.sleep }}
